# Folio v0.2 前向案例

在新的临时 Git 仓库中安装完整本地 Skill。每个案例使用独立的 `codex exec --ephemeral --skip-git-repo-check --sandbox read-only` 会话；Agent 可按 `SKILL.md` 条件读取完整 references。只提供对应案例的用户请求，不提供预期答案正文、Skill 名称或验收断言。结果记录在本次验收报告中，不把易过期的 PASS 快照提交进 Skill。

## 1. 完整内容套件

**用户请求**：

> 把下面短文写成公众号文章，并给我 4 张配图规格：某团队把每周的客服问题按主题归类。第一周有 18 个问题，第二周有 12 个；他们发现“登录失败”在两周都出现。团队决定先为登录流程补充帮助页，而不是宣称问题已经解决。

**期望触发**：触发 Folio，默认交付三产物。

**断言**：有 `publish-info`、`article-draft`、`figure-spec`；每张图的事实性文字都能在文章中逐字或等义定位。`publish-info` 有 2–3 个可复用的平台分类标签，且至少一个标签表达客服、服务运营、流程改进或语义相近的领域分类；标签不得包含 `18`、`12`、`登录失败` 或完整标题短语。

**失败条件**：缺少任一产物，或图中出现文章没有的数字、因果、结果或主张。

**Style 回归检查**：若选择半调拼贴，「套图设置」使用 `Halftone Paper Collage`；中文完整生图指令使用“半调网点与纸张拼贴”等自然中文描述，不把 `Halftone Paper Collage` 当作风格前缀，也不出现 `System v2` 或其他内部 Profile 版本。

## 2. 项目采用决策

**用户请求**：

> 用 https://github.com/RealKai42/qwerty-learner 写一套项目推荐文章和 4 张配图规格，帮助开发者判断是否采用。

**期望触发**：触发 Folio；项目或工具内容一次生成采用决策型三产物。

**断言**：文章直接包含来源支持的 walkthrough、前置条件、限制、best-fit 与 not-fit；证据与编辑判断可区分；`publish-info` 和 `figure-spec` 与文章同步。`publish-info` 有 2–3 个平台分类标签，且不得包含 `Qwerty Learner`、`英文输入训练` 或 `输错后重新输入` 等具体机制短语；`工具推荐`、`开源项目`、`开发者必看` 可以出现，但不是固定答案。

**失败条件**：先交基础版再要求升级，出现升级询问，或把 README 功能列表当成推荐文章。

## 3. 数字与公式保真

**用户请求**：

> 为下面内容制作一篇文章和 4 张配图规格：温度从 20 °C 升到 35 °C，变化量是 15 °C。面积公式为 A = πr²，半径 r 的范围是 2–4 cm。不要把单位或公式改写。

**期望触发**：触发 Folio。

**断言**：文章与每张图中出现的数字、单位、范围和 `A = πr²` 完全一致。标签不得包含 `20 °C`、`35 °C`、`15 °C`、`A = πr²` 或 `2–4 cm`，且至少一个标签属于数学、科学知识、知识科普或语义相近的平台分类。

**失败条件**：改变数字或单位，或以未声明的等价形式改写公式。

## 4. 负向触发

**用户请求**：

> 把下面内容整理成一篇学习笔记，不要配图：HTTP 缓存通过新鲜度和验证器避免不必要的重复传输。

**期望触发**：不隐式触发 Folio。

**断言**：只按普通请求生成学习笔记，不输出 `publish-info` 或 `figure-spec`。

**失败条件**：调用 Folio，或输出完整内容套件中的元数据或配图规格。

## 5. 跨工具 3:4 画布约束

**用户请求**：

> 把下面知识点制作成一篇短文和 4 张 3:4 竖版配图规格：可靠的自动化要区分输入验证、执行结果和失败恢复。

**期望触发**：触发 Folio；输出跨生图工具可交接的明确画布约束。

**断言**：「套图设置」给出 `3:4 竖版（宽:高 = 3:4）` 与 `1536×2048 px`；每条完整生图指令重复同一画幅和建议画布，以最高优先级 3:4 画布约束开头，明确禁止 2:3、4:5、1:1 和横版，并以严格 3:4 原生画布收束。「生成交接」要求支持比例或尺寸参数的工具显式设置目标值且不使用 Auto，对 prompt-only 工具不承诺覆盖其固定比例，并为不支持参考图的工具保留 Suite Lock + Prompt 退化路径。

**失败条件**：任一处出现 `1024×1536 px`、其他冲突比例或像素；缺少首尾画布约束；只依赖 prompt；或声称所有生图工具都能保证 3:4。

## 6. 相同关系的意义载体路由

分别在两个独立会话中运行以下请求。

**请求 A**：

> 把下面知识点写成一篇短文和 4 张配图规格：第一次提交报销时，票据这一对象依次经过收集、填写信息和提交审批三个步骤，前一步的同一份材料继续进入下一步。不要补充票据种类、填写字段、公司制度、人物或审批结果。

**请求 B**：

> 把下面知识点写成一篇短文和 4 张配图规格：报销请求依次处于材料待齐、信息已填、等待审批三个状态；材料未齐时停留在材料待齐，材料齐备且信息填写完成后进入信息已填，提交请求后进入等待审批。请让读者看清状态之间的边界和转化，不补充审批结果或其他状态。

**期望触发**：两者都是顺序关系，但 A 适合让同一对象沿连续手绘路径变化，自动选择 `Hand-Drawn Infographic`；B 依赖离散状态边界与转化，自动选择 `Halftone Paper Collage`。

**断言**：两份路由理由都说明所选媒介的表达优势、关键视觉机制和另一种 Style 所需的额外转换；不能只以“都是顺序关系”作为选择依据。A 不添加人物，仍以对象、路径和注释构成合格手绘信息图。两份输出在未指定比例时都使用 `3:4 竖版（宽:高 = 3:4）` 与 `1536×2048 px`。

**失败条件**：两份请求因信息关系相同而机械选择同一 Style；A 因没有人物而拒绝 Hand-Drawn 或补造人物；理由只写题材、信息关系或人物有无；Hand-Drawn 回退到 4:3；或任一输出缺少严格 3:4 约束。

## 7. 用户 Style 指定覆盖路由

**用户请求**：

> 把下面知识点写成一篇短文和 4 张配图规格，明确使用 Hand-Drawn Infographic：系统把请求分成必填输入、可选参数和明确排除项。使用对象、注释或空间关系解释，不要添加人物，也不要为三类补造具体条目、效果或判断标准。

**期望触发**：内容天然适合结构驱动表达，但用户指定受支持 Style，必须直接采用手绘；不得自动改回拼贴。

**断言**：「套图设置」和每条完整生图指令均使用 `Hand-Drawn Infographic` 对应的自然风格描述；未指定画幅时仍统一为 `3:4 竖版（宽:高 = 3:4）` 与 `1536×2048 px`。

**失败条件**：覆盖用户指定、混用 Style、因为没有人物而拒绝手绘、补造泛化讲解员、输出 4:3，或在产物中暴露内部分类名。

## 8. 视觉 QA mutation

对任一已生成的 4 张套图规格分别注入以下变异，并重新执行 Figure Spec QA：

1. 四张图都改成“顶部标题 + 中间主体 + 底部横条”。
2. 在参考图片内容没有提供的情况下加入可读终端命令、实验 ID 或状态列表。
3. 文案声明三个步骤，但构图只给两个可辨识位置。
4. 在中性 best-fit / not-fit 比较中，把一侧设为高饱和精致材质，另一侧设为灰暗粗糙材质。

**断言**：四种变异均标记 `FAIL`，并分别指出宏观模板重复、白名单外文字、数量不完整和无来源的颜色偏置。

**失败条件**：任一变异只得到 `WARN` 或被整套 `PASS` 覆盖。

## 9. 开源教程书身份连续性

分别在两个独立会话中运行同一请求：一次不指定 Style，一次明确指定 `Hand-Drawn Infographic`。

**用户请求**：

> 把下面事实制作成一篇短文和 5 张配图规格：《Agent 工程实践》是一部公开托管在 GitHub 的开源 AI Agent 教程书，内容讲解 LLM、上下文、工具、评估和多 Agent 协作。它以章节组织概念与工程练习。不要补充作者、Star 数、许可证名称、读者评价或实验结果。

**期望触发**：触发 Folio；Style 只改变表达机制，不改变来源名称、开源教程书身份或 AI Agent 主题。

**断言**：首图明确呈现《Agent 工程实践》、开源 AI Agent 教程书与主题；每张内页在「必要署名 / 限制」和完整生图指令中逐字包含最小来源标识“《Agent 工程实践》 · 开源 AI Agent 教程书”，且不把“未提供可核查来源”、核查日期或不确定性尾注复制成图上署名；「视觉方案与事实边界」及完整 Prompt 明确区分至少 1 个来源身份锚点与至少 2 个内容解释锚点；整套至少结合文字署名与书页、章节、阅读或工程练习中的一种非页脚身份机制。两次运行均保持相同身份事实；Hand-Drawn 可以只用书页、章节标注和工程练习对象，不要求加入读者或讲解员。

**失败条件**：只有首图能识别来源；内页退化为普通 AI Agent 海报；只重复固定页脚或书本中心构图；把 GitHub 托管扩写成未提供的许可证、Star 数或背书；或 Style 改变来源身份。

## 10. 来源身份 QA mutation

对案例 9 的任一合格输出分别注入以下变异，并重新执行 Figure Spec QA：

1. 删除书名、开源教程书描述与全部出版物线索，只保留 LLM、工具、代码和终端等通用 AI 图形。
2. 将一个只被来源称为“项目文档”的对象改写成“开源教程书”。
3. 五张图都使用相同的居中书本、相同页脚和相同标题位置，只替换章节文案。
4. 把《Agent 工程实践》及“开源 AI Agent 教程书”替换为一个不同主题的 AI Agent API 产品，只更新名称与最小来源标识，不调整原有书页、章节、阅读、工程练习等身份机制和内容解释锚点。

**断言**：四种变异均标记 `FAIL`，并分别指出 Recognition Test 失败、身份事实越界、身份机制与宏观模板重复、Generic Substitution Test 失败。若只换成另一部同主题 AI Agent 教程书，不得仅因缺少来源未提供的独有 Logo、资产或视觉特征而判定 `FAIL`。

**失败条件**：任一变异只得到 `WARN`，或仅因存在项目名、GitHub Logo、书本图标或固定页脚而通过。

## 11. 自动路由与统一视觉系统

分别在两个独立会话中运行以下请求。

**请求 A**：

> 把下面事实写成一篇短文和 5 张配图规格，不指定 Style：某个产品管理 Skill Marketplace 收录 68 个 Skill、42 个工作流和 9 个插件。使用流程先判断正在处理的是新产品还是已有产品，再发散想法并筛选 3–5 个候选，识别关键假设，按影响与风险排序，设计实验，最后根据真实决策判断是否安装一个领域插件；同一张决策记录会沿流程持续补充。流程中设置两次人工 checkpoint，每次预计 15–30 分钟。不要补充人物、具体插件名、实验结果或采用结论。

**请求 B**：

> 把下面知识点写成一篇短文和 4 张配图规格，不指定 Style：请求被分成必填输入、可选参数和排除项；三者有明确边界，参数不能跨越边界，排除项始终留在系统之外。不要补充具体参数。

**断言**：A 自动选择 `Hand-Drawn Infographic`，理由基于同一决策记录、注释和路径的连续手绘表达，不因没有人物或题材是“产品系统”而改选拼贴；B 自动选择 `Halftone Paper Collage`，理由基于离散边界、包含和排除关系。两份输出均在文件头给出具体「统一视觉系统」与背景选择依据；每条完整生图指令逐项完整重复背景、选择依据、颜色角色、材质、笔触或网点、光影、字体性格与适用的连续性约束，允许自然语序不同但不允许值缺失或改义。逐图不再输出独立「色彩、材质与氛围」字段。

**视觉漂移 mutation**：对 A 的任一合格输出分别把一张图的背景改成另一色族、交换主色与警示色的语义、改变重复决策记录的造型主色、或把既定表面改成光滑 3D 渲染；其余图片不变后重新执行 Figure Spec QA。

**失败条件**：A 因没有人物而自动选择 Halftone 或补造人物；B 用泛化人物替代结构边界；缺少统一视觉系统或背景选择依据；单图使用“同上”而未自包含；任一视觉漂移 mutation 未标记 `FAIL`；或声称参考色能保证不同生图工具像素级一致。

## 12. 混合 Style 请求边界

**用户请求**：

> 为同一篇文章制作 5 张配图：前两张使用 Hand-Drawn Infographic，后三张使用 Halftone Paper Collage。

**断言**：不直接生成混合 Style 的单套 `figure-spec`；说明两种 Style 使用不同视觉媒介，要求用户选择一种，或明确拆成两套并分别建立 Suite Lock。

**失败条件**：在同一套图中逐图路由、用一个统一视觉系统覆盖两种媒介，或未获得选择便直接替用户决定一种 Style。

## 13. 背景与人物不由 Style 默认决定

分别在独立会话中运行以下请求：

**请求 A**：

> 把下面事实写成一篇短文和 4 张配图规格，明确使用 `Hand-Drawn Infographic`：Blue Arc 是一个服务状态看板；来源提供的可核查视觉资产使用冷蓝 `#2457A6`、浅蓝白 `#EAF4FF` 和深蓝文字 `#17345C`。状态事实只有：正常表示所有已列服务按预期运行；受影响表示至少一个已列服务受到影响，但不表示全部服务不可用；恢复表示此前受影响的服务已经恢复到正常。允许的视觉机制只有一组数量不代表真实服务数量的无文字抽象服务卡片：正常时卡片保持统一默认轮廓；受影响时至少一张卡片使用加粗冷蓝外框，其余卡片保持默认轮廓；恢复时用同一张卡片从加粗外框回到默认轮廓的前后对照。不得使用连接线、阻塞、速度或告警隐喻。使用对象和注释解释这三个状态，不出现人物，不补充服务名称、数量、指标、事件、原因、时长或处理过程。

**请求 B**：

> 把“请求进入、验证、执行、返回”四步写成一篇短文和 4 张配图规格，明确使用 `Hand-Drawn Infographic`。发布平台要求深色高对比画布；使用手绘路径、对象和符号，不出现人物，不补充协议、耗时或结果。

**请求 C**：

> 把“读一页、写一句摘要、标一个问题”写成一篇短文和 3 张配图规格，明确使用 `Hand-Drawn Infographic`。我指定暖米白纸张以匹配自己的历史学习笔记；允许在动作确实需要时出现手，但不要求完整人物，不补充学习效果。

**请求 D**：

> 把下面事实写成一篇短文和 4 张配图规格，明确使用历史名称 `Hand-Drawn Cartoon Infographic`：Blue Arc 是一个服务状态看板；来源提供的可核查视觉资产使用冷蓝 `#2457A6`、浅蓝白 `#EAF4FF` 和深蓝文字 `#17345C`。状态事实只有：正常表示所有已列服务按预期运行；受影响表示至少一个已列服务受到影响，但不表示全部服务不可用；恢复表示此前受影响的服务已经恢复到正常。允许的视觉机制只有一组数量不代表真实服务数量的无文字抽象服务卡片：正常时卡片保持统一默认轮廓；受影响时至少一张卡片使用加粗冷蓝外框，其余卡片保持默认轮廓；恢复时用同一张卡片从加粗外框回到默认轮廓的前后对照。不得使用连接线、阻塞、速度或告警隐喻。使用对象和注释解释这三个状态，不出现人物，不补充服务名称、数量、指标、事件、原因、时长或处理过程。

**断言**：前三份输出分别选择冷色浅背景、深色高对比背景和暖米白纸张，并在「统一视觉系统」中写明来源、平台或用户依据；都不因为 Hand-Drawn 自动添加人物。请求 A 与 D 的全部图上文字、核心主张与状态关系只表达输入给出的三条状态定义；视觉机制只使用输入允许的无文字抽象服务卡片、外框变化与恢复前后对照，不补造服务名称、数量、响应速度、连接或阻塞状态、告警、原因、时长、事件、处理或恢复过程。请求 D 被兼容接受，但「套图设置」使用规范名称 `Hand-Drawn Infographic`，每条 Prompt 使用自然中文风格描述且不泄露历史名称。

**失败条件**：前三份都回到暖米白或复古纸；只写“因为是手绘”作为背景依据；无人物请求被补造讲解员、产品经理或工程师；请求 A 或 D 补造输入未给出的状态含义或运营事实；历史名称继续出现在用户产物。

## 14. 精简 Figure Spec 与内容原生推进

**用户请求**：

> 把下面内容写成一篇文章和 4 张社交媒体配图规格：做决策时先确认目标，再列出明确约束，比较可逆选项，最后选择最低成本下一步。不要添加收藏、关注、转发、评论或“下一页更精彩”等互动文案，也不要补充数字效果。

**断言**：文件头只有「套图设置」「统一视觉系统」「生成交接」；每张图只有「参考图片内容」「本图任务」「视觉方案与事实边界」「完整生图指令」，不出现旧「配图规格」字段清单。「本图任务」用短段落表达位置角色、认知目标、核心主张与信息关系；审校摘要中的构图、锚点、安全区和事实边界都能在同图 Prompt 中定位。每条 Prompt 可独立生图，不依赖文件头、“同上”或其他图片。整套通过真实问题、机制、判断和下一步自然推进，不自动添加互动文案、FOMO、无依据最高级、虚假社会证明或数字收益。

**失败条件**：继续逐图重复 Style、画幅、画布、下游执行、语言或色材字段；摘要拥有 Prompt 中没有的约束；任一 Prompt 不自包含；或自动加入“必看”“99%”“错过”“收藏备用”“评论区见”等输入未提供的文案。

## 15. 生成交接、明确 CTA 与修正路径

**用户请求**：

> 把“准备输入、执行动作、核对结果”写成一篇短文和 3 张配图规格。最后一张明确写“收藏备用”，不要改写，也不要添加关注、转发或评论文案。

**断言**：“收藏备用”先作为用户提供的非事实型编辑文案纳入文章，再在最后一张「参考图片内容」和 Prompt 中逐字出现；不得扩写为稀缺、效果或社会证明。「生成交接」说明参考图支持是条件式的：先生成图 1，图 2+ 只继承媒介、颜色渲染和重复对象造型，当前白名单与 Prompt 优先，参考图不是内容证据；工具不支持参考图时退化为 Suite Lock + Prompt；Prompt 与参考图不能替代 Bitmap QA。成图有误时要求修正 Prompt 后重新生成，不允许程序覆盖位图文字。

**QA mutation**：分别把图 1 的标题作为参考图内容复制进图 2、让图 2 沿用图 1 的宏观构图、删除无参考图退化路径、以及声明用程序覆盖错误文字；四种变异都必须标记 `FAIL`。

**失败条件**：自动添加其他 CTA；把“收藏备用”增强为效果或 FOMO；参考图凌驾于当前 Prompt；参考图成为强制依赖；或任一 mutation 未标记 `FAIL`。

## 16. Background Role Router 分流

分别在独立会话中运行以下请求：

**请求 A（高密度阅读）**：

> 把“安装依赖、设置环境变量、运行检查、读取错误、修正后复验”写成一篇短教程和 5 张配图规格。每张都有命令或步骤文字，不指定 Style、背景或颜色，不补造具体命令。

**请求 B（低密度聚焦）**：

> 把“可逆，才值得先试”写成一篇短文和 3 张低密度配图规格。不指定 Style、背景或颜色，不添加数字、案例或互动文案。

**请求 C（明确暖纸）**：

> 把“读一页、写一句摘要、标一个问题”写成一篇短文和 3 张配图规格。我指定暖米白纸张，以匹配自己的历史学习笔记。

**请求 D（来源视觉）**：

> 只根据以下输入写一篇短文和 3 张配图规格，不检索或补充其他项目事实：Blue Arc 是一个服务状态看板；可核查来源视觉使用冷蓝 `#2457A6`、浅蓝白 `#EAF4FF` 和深蓝文字 `#17345C`。不要改成其他品牌色或纸张色，不补充服务、事件、指标或状态事实。

**断言**：四份「选择依据」均用一句紧凑结论写明背景职责、输入证据及为什么胜过最接近的候选。A 优先满足高密度阅读与稳定对比，可选择冷白、浅灰或低彩浅色，但不能因为高密度机械写成米白；B 必须认真比较彩色场或深色聚焦底，不能只以“亲和、手绘、纸张感”回到浅暖纸；C 保留用户指定的暖米白，并说明其来源底或情绪底职责；D 优先延续来源视觉。四份结果不得全部落在同一背景家族，同一套内部不得逐图换底。

**失败条件**：内部三个候选都是近似浅暖色；选择依据只有 Style、题材或泛化情绪词；A–D 全部输出米白；B 未比较彩色或深色候选；C 因反米白规则覆盖用户指定；D 用通用米白覆盖来源色；或为追求多样而逐图换背景。
