# Figure Spec 生图前 QA
# Figure Spec 生图前 QA


只检查规格，不生成图片，也不替代 Bitmap 后验 QA。先逐图检查，再检查整套；任一硬失败都必须修正后重检。
只检查规格，不生成图片，也不替代 Bitmap 后验 QA。先逐图检查，再检查整套；任一硬失败都必须修正后重检。


## 严重级别
## 严重级别


- `FAIL`：事实越界、缺少 Style/画幅/语言/必需文字、依赖全局上下文、文字不可读风险、虚假证据或错误视觉关系。
- `FAIL`：事实越界、缺少 Style/画幅/语言/必需文字、依赖全局上下文、文字不可读风险、虚假证据或错误视觉关系。
- `WARN`：信息偏密、层级弱、构图重复、留白职责不清或 Style 机制不足。
- `WARN`：信息偏密、层级弱、构图重复、留白职责不清或 Style 机制不足。
- `PASS`：约束完整且可直接生图。
- `PASS`：约束完整且可直接生图。


最终 `PASS` 是硬门槛：必须为零个 `FAIL`，且每个 `WARN` 都已经修正或以可核查证据说明不构成风险。不能用“整体可用”覆盖单图失败。
最终 `PASS` 是硬门槛：必须为零个 `FAIL`，且每个 `WARN` 都已经修正或以可核查证据说明不构成风险。不能用“整体可用”覆盖单图失败。


## 可重复执行的硬门槛
## 可重复执行的硬门槛


先把每个 `## 图 N` 到下一张图之前的内容视为一张独立任务包，不继承文件开头或其他图片的信息。逐包执行：
先把每个 `## 图 N` 到下一张图之前的内容视为一张独立任务包，不继承文件开头或其他图片的信息。逐包执行：


| 门槛 | 通过条件 | 失败证据 |
| 门槛 | 通过条件 | 失败证据 |
| --- | --- | --- |
| --- | --- | --- |
| 固定结构 | 同时存在「参考图片内容」和「配图规格」 | 任一小节缺失 |
| 固定结构 | 同时存在「参考图片内容」和「配图规格」 | 任一小节缺失 |
| 必需字段 | Style、画幅、语言、认知目标、核心主张、信息关系、构图机制、视觉锚点、色材、文字安全区、禁区事实边界、完整生图指令均有实际值 | 字段缺失、为空，或仍含方括号占位符 |
| 必需字段 | Style、画幅、语言、认知目标、核心主张、信息关系、构图机制、视觉锚点、色材、文字安全区、禁区事实边界、完整生图指令均有实际值 | 字段缺失、为空，或仍含方括号占位符 |
| 实际文字 | 主标题及所有需要出现的正文、强调词、限制均逐项写明；没有则明确写“无” | “见上文”“同上”“略”等上下文引用 |
| 实际文字 | 主标题及所有需要出现的正文、强调词、限制均逐项写明；没有则明确写“无” | “见上文”“同上”“略”等上下文引用 |
| 自包含 Prompt | 完整生图指令重申 Style、比例方向、语言、核心视觉机制、文字避让与事实禁区 | 只写“按上述生成”或依赖文件级声明 |
| 自包含 Prompt | 完整生图指令重申 Style、比例方向、语言、核心视觉机制、文字避让与事实禁区 | 只写“按上述生成”或依赖文件级声明 |
| 套图一致性 | 每张 Style 与统一 Style 相同，每张画幅与统一比例相同 | 无用户指示却混用 |
| 套图一致性 | 每张 Style 与统一 Style 相同，每张画幅与统一比例相同 | 无用户指示却混用 |
| Style 专项 | 手绘图说明概念解释元素与留白职责；半调图说明主导编辑操作、关系、至少 2 个项目原生锚点、文字—物体联动、removal delta | 只有风格形容词或纹理说明 |
| Style 专项 | 手绘图说明概念解释元素与留白职责；半调图说明主导编辑操作、关系、至少 2 个项目原生锚点、文字—物体联动、removal delta | 只有风格形容词或纹理说明 |


人工语义检查不能被字段检索替代。字段检索只能证明结构存在，事实边界、文字负载、编辑操作是否真正承载关系仍需逐图判断。
人工语义检查不能被字段检索替代。字段检索只能证明结构存在，事实边界、文字负载、编辑操作是否真正承载关系仍需逐图判断。


## 逐图检查
## 逐图检查


1. **事实边界**：核心主张、数字、公式、名称、命令、URL 与限制均能追溯到 `article-draft` 或来源；没有新增效果、排名、关系或证据。
1. **事实边界**：核心主张、数字、公式、名称、命令、URL 与限制均能追溯到 `article-draft` 或来源；没有新增效果、排名、关系或证据。
2. **参考图片内容**：主标题、正文、强调词与必要限制完整明确；没有用“同上”或省略号代替实际内容。
2. **参考图片内容**：主标题、正文、强调词与必要限制完整明确；没有用“同上”或省略号代替实际内容。
3. **自包含性**：单独复制该图时，仍有完整 Style 名称与版本、画幅、方向、语言、文字、认知目标、关系、构图、锚点、色材、禁区、安全区和完整生图指令。
3. **自包含性**：单独复制该图时，仍有完整 Style 名称与版本、画幅、方向、语言、文字、认知目标、关系、构图、锚点、色材、禁区、安全区和完整生图指令。
4. **文字负载**：主张 5 秒内可扫读；正文可在目标画幅中留出合理字号与行距。无法排下时先压缩文案或拆图。
4. **文字负载**：主张 5 秒内可扫读；正文可在目标画幅中留出合理字号与行距。无法排下时先压缩文案或拆图。
5. **层级与安全区**：标题、主体、辅助说明有明确顺序；文字不贴边，不被人物、图标、裁切线、纸边、连接线、阴影或边框覆盖。
5. **层级与安全区**：标题、主体、辅助说明有明确顺序；文字不贴边，不被人物、图标、裁切线、纸边、连接线、阴影或边框覆盖。
6. **比例**：画幅与全套声明一致；如不一致，必须有用户明确要求。
6. **比例**：画幅与全套声明一致；如不一致，必须有用户明确要求。
7. **Style 参与表达**：
7. **Style 参与表达**：
   - 手绘：人物/图标直接解释概念，留白与关键词承担信息层级；不是手绘装饰围住文字。
   - 手绘：人物/图标直接解释概念，留白与关键词承担信息层级；不是手绘装饰围住文字。
   - 半调拼贴：声明可观察的编辑操作、对应信息关系、至少 2 个可见项目原生锚点、文字—物体联动与 removal delta；不是纹理背景。
   - 半调拼贴：声明可观察的编辑操作、对应信息关系、至少 2 个可见项目原生锚点、文字—物体联动与 removal delta；不是纹理背景。
8. **视觉锚点**：元素具体、可执行、与主题或项目相关；泛化科技符号不能独自承担身份。
8. **视觉锚点**：元素具体、可执行、与主题或项目相关；泛化科技符号不能独自承担身份。
9. **禁区**：明确禁止可能造成误读的假截图、假数据、假背书、假流程、受保护肖像复制或多余文字。
9. **禁区**：明确禁止可能造成误读的假截图、假数据、假背书、假流程、受保护肖像复制或多余文字。


## 整套检查
## 整套检查


1. 只使用一个已选 Style，除非用户明确要求混用。
1. 只使用一个已选 Style，除非用户明确要求混用。
2. 画幅、语言、基础色彩和材质连续。
2. 画幅、语言、基础色彩和材质连续。
3. 社交媒体套图提炼 4–6 个高价值信息点；教程/报告覆盖全部主要章节。
3. 社交媒体套图提炼 4–6 个高价值信息点；教程/报告覆盖全部主要章节。
4. 相邻图片不重复同一标题位置、中心物体、页脚标签、围合深度和密度。
4. 相邻图片不重复同一标题位置、中心物体、页脚标签、围合深度和密度。
5. 半调套图复用视觉词汇而非完成拼贴；交换相邻主构图后若仍同样易懂，标记 `FAIL`。
5. 半调套图复用视觉词汇而非完成拼贴；交换相邻主构图后若仍同样易懂，标记 `FAIL`。
6. 所有图的认知目标互补，不是重复改写同一句话。
6. 所有图的认知目标互补，不是重复改写同一句话。


## QA 输出
## QA 输出


```markdown
```markdown
## Figure Spec QA
## Figure Spec QA


| 图号 | 结果 | 问题 | 修正 |
| 图号 | 结果 | 问题 | 修正 |
| --- | --- | --- | --- |
| --- | --- | --- | --- |
| 图 1 | PASS / WARN / FAIL | [证据] | [动作] |
| 图 1 | PASS / WARN / FAIL | [证据] | [动作] |


**整套结果**：PASS / FAIL
**整套结果**：PASS / FAIL
**必须修正**：[没有则写“无”]
**必须修正**：[没有则写“无”]
**复检证据**：[修正后的字段或关系，能够定位到具体图]
**复检证据**：[修正后的字段或关系，能够定位到具体图]
```
```


只把 QA 作为内部检查记录。完成修正后仅向用户交付三份正式产物。
只把 QA 作为内部检查记录。完成修正后仅向用户交付三份正式产物。


保存临时 QA 证据时至少记录：输入文件路径、检查时间、所选 Style 与统一比例、逐图结果、整套结果。QA 记录不是用户交付物。
保存临时 QA 证据时至少记录：输入文件路径、检查时间、所选 Style 与统一比例、逐图结果、整套结果。QA 记录不是用户交付物。


## 故障注入回归
## 故障注入回归


验证 QA 时，复制一张合格规格到临时位置，故意删除 Style 或画幅：
验证 QA 时，复制一张合格规格到临时位置，故意删除 Style 或画幅：


- 缺 Style → 自包含性检查必须 `FAIL`；
- 缺 Style → 自包含性检查必须 `FAIL`；
- 缺画幅 → 自包含性与比例检查必须 `FAIL`；
- 缺画幅 → 自包含性与比例检查必须 `FAIL`；
- 还原字段后复检必须 `PASS`。
- 还原字段后复检必须 `PASS`。


故障注入只能操作临时副本；不得破坏正式模板或三产物样例。证据必须同时包含失败时命中的图号/字段，以及还原后同一检查的 `PASS`。
故障注入只能操作临时副本；不得破坏正式模板或三产物样例。证据必须同时包含失败时命中的图号/字段，以及还原后同一检查的 `PASS`。
