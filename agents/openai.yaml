interface:
  display_name: "Folio"
  short_description: "Create social-ready copy and visual briefs"
  default_prompt: "Use $folio for social copy and visual briefs by default. Return only article-draft for a standalone article request; include all three outputs only for a combined or full package."
policy:
  products:
  - chatgpt
  - codex
  - atlas
  allow_implicit_invocation: true
